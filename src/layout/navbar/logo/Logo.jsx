import { Typography } from '@mui/material'
import React from 'react'

export default function Logo({ webTitle }) {
    return (
        <Typography>
            {webTitle}
        </Typography>
    )
}
